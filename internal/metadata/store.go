package metadata

// MetadataStore defines the Phase 1 persistence interface.
// It uses engine types via the engine package's own MetadataStore interface.
// This package provides the implementation only.
